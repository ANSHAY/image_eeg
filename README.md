# Visual Cortex Reconstructor

Reconstructs the image a person was viewing from their EEG signal — fully offline, on a consumer laptop with no discrete GPU.

A 0.5 s, 128-channel EEG trial is streamed over [LSL](https://github.com/sccn/labstreaminglayer), encoded into the CLIP latent space by a 1D-CNN, and then either retrieved against a CLIP image bank or generated by SD-Turbo + IP-Adapter. The result is a Streamlit split-screen demo: live brainwaves on one side, an AI reconstruction of the perceived image on the other.

## Status

Phase 0 (bootstrap) in progress. See [docs/implementation_plan.md](docs/implementation_plan.md) §11 for the atomic checklist — the first unchecked `[ ]` is the next commit.

## Hardware target

- Intel Core Ultra 5 225H (CPU + integrated NPU, integrated graphics)
- 32 GB DDR5, no discrete GPU
- Linux (Arch-based)
- ~100 GB free disk

Inference must run fully offline. CLIP and SD-Turbo are downloaded once during setup, then frozen.

## Setup

The project assumes you've already created a Python 3.14 virtual environment at `./.venv/`. If not:

```bash
python3.14 -m venv .venv
```

Then run the idempotent bootstrap:

```bash
./setup.sh
```

This installs pinned dependencies (CPU-only torch, transformers 4.x, diffusers 0.38, OpenVINO 2026.1, …), downloads the EEG dataset (Spampinato primary, THINGS-EEG2 fallback) and the 40-class ImageNet stimulus subset, then runs a smoke-import check.

On Arch Linux, `pylsl` benefits from a system `liblsl` install:

```bash
sudo pacman -S liblsl
```

If absent, pylsl falls back to its wheel-bundled library — works, but the system package is preferred.

## Run

Commands below land as their phases ship. Today only the env is bootstrapped.

```bash
# Phase 1 — Virtual EEG rig (LSL streamer + receiver)
.venv/bin/python -m streaming.lsl_streamer --speed 1.0
.venv/bin/python -m streaming.lsl_receiver

# Phase 2 — Offline preprocessing
.venv/bin/python -m preprocessing.run_pipeline

# Phase 3 — Train the EEG → CLIP encoder
.venv/bin/python -m models.train

# Phase 6 — Demo app
.venv/bin/streamlit run app/streamlit_app.py
```

## Architecture

See [docs/knowledge_graph.md](docs/knowledge_graph.md) for the architecture diagram, module map, and design-decision log. The full source spec is [docs/project_3a_visual_cortex_reconstructor.md](docs/project_3a_visual_cortex_reconstructor.md); the executable plan is [docs/implementation_plan.md](docs/implementation_plan.md).

## Configuration

All runtime parameters — paths, model IDs, hyperparameters, UI strings, colors, filter cutoffs — live in [config.yaml](config.yaml). Code never references literal values; the [`utils.config`](utils/config.py) loader returns a frozen, pydantic-validated `Config` object, and `tests/test_no_magic_strings.py` enforces this in CI.

## Results

To be populated once Phase 3 completes the LOSO-CV training run. Ship gates:

| Metric | Target |
|---|---|
| Top-5 retrieval (LOSO held-out) | ≥ 50% |
| Qualitative semantic match | ≥ 70% of 20-sample manual review |
| End-to-end latency (SD-Turbo) | < 60 s |
| End-to-end latency (retrieval) | < 2 s |

## License

TBD.
